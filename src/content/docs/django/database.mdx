---
title: Base de données
lastUpdated: 2024-10-16
description: Un guide pour modifier la base de données d'un projet Django.
sidebar:
    order: 4
    label: Base de données
---

import { Steps } from '@astrojs/starlight/components';

## SQLite

Configuration initiale d'un projet Django.

## PostgreSQL

:::note[Documenation]
[psycopg[binary]](https://www.psycopg.org/psycopg3/docs/basic/install.html)
:::

<Steps>
1. ### Installation

    ```sh
    pip install "psycopg[binary]"
    ```

2. ### Configuration

    Modifier le fichier `settings.py`.

    ```diff lang="python"
    // settings.py

    ...
    -DATABASES = {
    -    'default': {
    -        'ENGINE': 'django.db.backends.sqlite3',
    -        'NAME': BASE_DIR / 'db.sqlite3',
    -    }
    -}

    +DATABASES = {
    +    'default': {
    +        'ENGINE': 'django.db.backends.postgresql',
    +        "NAME": os.environ['POSTGRES_DB'],
    +        "USER": os.environ['POSTGRES_USER'],
    +        "PASSWORD": os.environ['POSTGRES_PASSWORD'],
    +        "HOST": os.environ['POSTGRES_HOST'],
    +        "PORT": os.environ['POSTGRES_PORT'],    
    +    }
    +}
    ...
    ```

3. ### Variables d'environnement

    ```env
    POSTGRES_DB="<database-name>"
    POSTGRES_USER="database-username>"
    POSTGRES_PASSWORD="<database-password>"
    POSTGRES_HOST="<database-host>"
    POSTGRES_PORT="<database-host>"
    ```
</Steps>

