name: CD

on:
  push:
    branches:
    - main
  workflow_dispatch:

jobs:
  deploy:
    permissions:
      contents: read
      pages: write
      id-token: write
    concurrency:
      group: "pages"
      cancel-in-progress: false
    uses: ./.github/workflows/deploy.yaml