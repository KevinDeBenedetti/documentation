name: CD

on:
  push:
    branches:
    - main
  workflow_dispatch:
    
jobs:
  deploy:
    permissions:
      contents: read
      pages: write
      id-token: write
    uses: ./.github/workflows/deploy.yaml